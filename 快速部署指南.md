# 彭城七里徐州旅游攻略 - 快速部署指南

## ✅ 已完成
代码已提交到本地 Git 仓库

## 📱 手动部署步骤（约3分钟）

### 第一步：打开 GitHub 仓库
在浏览器中打开：
```
https://github.com/zxz716604-sys/pengcheng-travel
```

### 第二步：推送代码到 GitHub
在 CMD 或 PowerShell 中执行：
```bash
cd e:\彭城七里徐州旅游攻略
git push origin master
```

**如果遇到认证问题：**
1. 访问 https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. 勾选 `repo` 权限
4. 点击 "Generate token"
5. 复制令牌，推代码时作为密码使用

### 第三步：开启 GitHub Pages
1. 在仓库页面，点击 **Settings**（设置）
2. 左侧菜单点击 **Pages**
3. 在 **Source** 下选择 **master** 分支
4. 点击 **Save**
5. 等待1-2分钟

### 第四步：获取永久链接
部署成功后，你会得到：
```
https://zxz716604-sys.github.io/pengcheng-travel/
```

---

## 📸 更新二维码

部署成功后，用上面的永久链接生成新二维码：

```python
import qrcode

url = "https://zxz716604-sys.github.io/pengcheng-travel/"
qr = qrcode.QRCode(version=1, error_correction=qrcode.constants.ERROR_CORRECT_H, box_size=10, border=4)
qr.add_data(url)
qr.make(fit=True)
img = qr.make_image(fill_color='brown', back_color='white').resize((400, 400))
img.save("微信访问二维码.png")
```

---

## 📱 微信访问

用微信扫描更新后的二维码，即可随时随地访问你的旅游攻略！

---

**问题？** 如果遇到问题，请截图发给我。
